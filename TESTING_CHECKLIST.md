# Чек-лист проверки перед показом

## Подготовка

### 1. Убедись что бэкенд запущен
```bash
cd /Users/vasilymaslovsky/Documents/lab6
docker-compose up -d
# или как у тебя запускается бэкенд
```

### 2. Узнай свой IP в локальной сети
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
# Запиши IP, например: 192.168.1.100
```

---

## Проверка 1: GitHub Pages + PWA на телефоне

### Шаг 1: Задеплой на GitHub Pages
```bash
cd /Users/vasilymaslovsky/Documents/lab6/frontend

# Убедись что target_tauri = false в src/target_config.ts
# Затем:
npm run build
npm run deploy
```

### Шаг 2: Открой на телефоне
1. Открой `https://vasilymsl.github.io/credit-scoring-system-/` в браузере телефона
2. Должна появиться страница с услугами (mock данные)
3. **Скрин 1:** Главная страница на телефоне

### Шаг 3: Установи PWA
- **Android:** Меню браузера → "Добавить на главный экран"
- **iOS:** Кнопка "Поделиться" → "На экран Домой"
- **Скрин 2:** Иконка приложения на рабочем столе

### Шаг 4: Открой PWA и проверь фильтр (Redux)
1. Открой установленное приложение
2. Перейди на "Скоринговые модели"
3. Введи что-нибудь в фильтр (например, название)
4. Нажми "Применить"
5. **Скрин 3:** Фильтр применён
6. Перейди на "Главная"
7. Вернись на "Скоринговые модели"
8. **Скрин 4:** Фильтр сохранился! (Redux работает)

---

## Проверка 2: Адаптивность в браузере

### Шаг 1: Открой DevTools
1. Открой `https://vasilymsl.github.io/credit-scoring-system-/scoring-models`
2. F12 → DevTools
3. Нажми на иконку "Toggle device toolbar" (или Ctrl+Shift+M)

### Шаг 2: Меняй ширину и скринь
- **1200px:** 3 карточки в ряд → **Скрин 5**
- **900px:** 2 карточки в ряд → **Скрин 6**
- **500px:** 1 карточка в ряд → **Скрин 7**

### Шаг 3: Покажи код
Открой `src/index.css` и покажи строки с media queries:
```css
/* Строка ~70: 2 колонки на планшете */
@media (max-width: 1024px) { ... }

/* Строка ~80: 1 колонка на мобильном */
@media (max-width: 768px) { ... }
```
**Скрин 8:** Код с media queries

---

## Проверка 3: Работа с бэкендом на компьютере

### Шаг 1: Запусти локально с бэкендом
```bash
cd /Users/vasilymaslovsky/Documents/lab6/frontend
npm run dev
```

### Шаг 2: Открой в браузере
1. Открой `https://localhost:3000/credit-scoring-system-/scoring-models`
2. Должны загрузиться реальные данные с бэкенда (не mock!)
3. **Скрин 9:** Данные с бэкенда в браузере

---

## Проверка 4: Tauri с бэкендом по IP

### Шаг 1: Настрой IP в конфиге
Открой `src/target_config.ts` и измени:
```typescript
export const target_tauri = true;
export const api_server_ip = "http://ТВОЙ_IP:8080";  // например 192.168.1.100
```
**Скрин 10:** Код с IP адресом

### Шаг 2: Пересобери Tauri
```bash
source "$HOME/.cargo/env"  # если нужно
npm run tauri build
```

### Шаг 3: Запусти приложение
```bash
open src-tauri/target/release/bundle/macos/ScoringApp.app
```

### Шаг 4: Проверь работу
1. Приложение должно открыться
2. Перейди на "Скоринговые модели"
3. Данные должны загрузиться с бэкенда
4. **Скрин 11:** Tauri приложение с данными

### Шаг 5: Сравни IP
- Покажи IP в консоли: `ifconfig`
- Покажи IP в коде: `src/target_config.ts`
- **Скрин 12:** Сравнение IP

---

## Проверка 5: Wireshark/tcpdump

### Вариант A: tcpdump (проще)
```bash
# В одном терминале запусти:
sudo tcpdump -i any port 8080 -n

# В другом терминале/в Tauri обнови страницу
# Увидишь пакеты с твоим IP
```
**Скрин 13:** Вывод tcpdump

### Вариант B: Wireshark
1. Открой Wireshark
2. Выбери сетевой интерфейс
3. Фильтр: `tcp.port == 8080`
4. Обнови страницу в Tauri
5. **Скрин 14:** Пакеты в Wireshark

---

## Проверка 6: Изменение в БД → обновление в Tauri

### Шаг 1: Измени что-то в БД
```bash
# Подключись к БД (например через psql или GUI)
# Измени название услуги:
    UPDATE credits SET title = 'ТЕСТ ИЗМЕНЕНИЯ' WHERE id = 1;
```

### Шаг 2: Обнови Tauri
1. В Tauri приложении обнови страницу (Cmd+R)
2. Увидишь изменённое название
3. **Скрин 15:** Изменённые данные в Tauri

---

## Проверка 7: HTTPS локально

### Шаг 1: Запусти dev сервер
```bash
npm run dev
```

### Шаг 2: Проверь HTTPS
1. Открой `https://localhost:3000`
2. Должен быть замочек 🔒 в адресной строке
3. **Скрин 16:** HTTPS в браузере

---

## Итого: 16 скринов

| # | Что скринить |
|---|--------------|
| 1 | PWA на телефоне - главная |
| 2 | Иконка PWA на рабочем столе |
| 3 | Фильтр применён |
| 4 | Фильтр сохранился после навигации |
| 5 | Адаптив 1200px - 3 колонки |
| 6 | Адаптив 900px - 2 колонки |
| 7 | Адаптив 500px - 1 колонка |
| 8 | Код media queries |
| 9 | Данные с бэкенда в браузере |
| 10 | Код с IP адресом |
| 11 | Tauri с данными |
| 12 | Сравнение IP (консоль vs код) |
| 13 | tcpdump или Wireshark |
| 14 | (опционально) Wireshark детально |
| 15 | Изменённые данные после UPDATE |
| 16 | HTTPS замочек |

---

## Если что-то не работает

### PWA не устанавливается
- Убедись что открываешь по HTTPS
- Очисти кеш браузера

### Tauri не видит бэкенд
- Проверь что бэкенд запущен
- Проверь IP адрес (ifconfig)
- Проверь что firewall не блокирует порт 8080

### Фильтр не сохраняется
- Открой Redux DevTools в браузере
- Проверь что state.filter обновляется

### Нет HTTPS
- Удали `node_modules/.vite` и перезапусти `npm run dev`

